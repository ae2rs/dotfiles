# Atuin — SQLite-backed shell history.
# Fully local: no account, no sync, no telemetry.

## Sync is inert without an account, but be explicit about it.
auto_sync = false

## The only network call Atuin makes by default (a release check).
update_check = false

## Ctrl-R searches everything, from every terminal and session.
filter_mode = "global"

## Ctrl-R is the only key that opens Atuin's search UI. 35-atuin.zsh
## passes --disable-up-arrow and rebinds Up to a widget reading this same
## database non-interactively, so Up recalls the previous command inline
## instead of opening a picker. That widget passes its own --filter-mode
## global, which is why filter_mode_shell_up_key_binding is absent here:
## it only ever applied to Atuin's own Up binding.

## fzf-style fuzzy: a bare term matches as a subsequence, so "cgo" finds
## "cargo". The fzf extended syntax works too -- 'exact, ^prefix, suffix$,
## !negate, | or -- plus r/regex/, which fzf has no equivalent for.
## Prefix a term with ' for exact matching; set "fulltext" to make that the
## default (the old FZF_CTRL_R_OPTS="--exact" behaviour).
search_mode = "fuzzy"

## Sync-only machinery; nothing to run in the background.
[daemon]
enabled = false

[ai]
enabled = false
